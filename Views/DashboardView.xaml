<UserControl x:Class="GroceryPOS.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:GroceryPOS.Converters"
             Background="{DynamicResource BgBrush}" FontFamily="{DynamicResource AppFont}">

    <UserControl.Resources>
        <conv:IsNegativeConverter x:Key="IsNegativeConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="30,24">
        <StackPanel>

            <!-- Greeting -->
            <Grid Margin="0,0,0,6">
                <TextBlock Text="{Binding Greeting}" FontSize="26" FontWeight="Bold"
                           Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="⏰" FontSize="24" Foreground="#1E293B" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock FontSize="26" FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryBrush}" Text="{Binding CurrentTime}"/>
                </StackPanel>
            </Grid>
            <TextBlock FontSize="13" Foreground="{DynamicResource SubTextBrush}" Margin="0,0,0,28">
                <Run Text="Today is "/>
                <Run Text="{Binding Source={x:Static System:DateTime.Now}, StringFormat='dddd, dd MMMM yyyy', Mode=OneWay}"
                     xmlns:System="clr-namespace:System;assembly=mscorlib"/>
            </TextBlock>

            <!-- Stats Cards -->
            <UniformGrid Columns="6" Margin="0,0,0,28">
                <!-- Total Sales Today -->
                <Border Style="{DynamicResource CardBorder}" Margin="0,0,6,0">
                    <StackPanel>
                        <TextBlock Text="💰" FontSize="26" Margin="0,0,0,8"/>
                        <TextBlock Text="Total Sales Today" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource SubTextBrush}"/>
                        <TextBlock FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <Binding Path="TodaySales" StringFormat="Rs.{0:N0}"/>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Foreground="{DynamicResource SubTextBrush}" FontSize="11" Margin="0,4,0,0">
                            <Run Text="{Binding TodaySaleCount, Mode=OneWay}" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                            <Run Text=" transactions"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Returns Today -->
                <Border Style="{DynamicResource CardBorder}" Margin="6,0">
                    <StackPanel>
                        <TextBlock Text="↩️" FontSize="26" Margin="0,0,0,8"/>
                        <TextBlock Text="Returns Today" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource SubTextBrush}"/>
                        <TextBlock FontSize="22" FontWeight="Bold" Foreground="#FF7043" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <Binding Path="TodayReturns" StringFormat="Rs.{0:N0}"/>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text="items returned today" Foreground="{DynamicResource SubTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Customer Credit (Due) -->
                <Border Style="{DynamicResource CardBorder}" Margin="6,0">
                    <StackPanel>
                        <TextBlock Text="💳" FontSize="26" Margin="0,0,0,8"/>
                        <TextBlock Text="Customer Credit" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource SubTextBrush}"/>
                        <TextBlock FontSize="22" FontWeight="Bold" Foreground="{DynamicResource DangerBrush}" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <Binding Path="TodayCredit" StringFormat="Rs.{0:N0}"/>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text="unpaid amount today" Foreground="{DynamicResource SubTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Recovered Credit (Previous Bills) -->
                <Border Style="{DynamicResource CardBorder}" Margin="6,0">
                    <StackPanel>
                        <TextBlock Text="💼" FontSize="26" Margin="0,0,0,8"/>
                        <TextBlock Text="Recovered Credit" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource SubTextBrush}"/>
                        <TextBlock FontSize="22" FontWeight="Bold" Foreground="#8B5CF6" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <Binding Path="TodayRecoveredCredit" StringFormat="Rs.{0:N0}"/>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text="from previous invoices" Foreground="{DynamicResource SubTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Cash In Drawer -->
                <Border Style="{DynamicResource CardBorder}" Margin="6,0">
                    <StackPanel>
                        <TextBlock Text="🏦" FontSize="26" Margin="0,0,0,8"/>
                        <TextBlock Text="Cash In Drawer" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource SubTextBrush}"/>
                        <TextBlock FontSize="22" FontWeight="Bold" Margin="0,4,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TodayCashInDrawer, Converter={StaticResource IsNegativeConverter}}" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <TextBlock.Text>
                                <Binding Path="TodayCashInDrawer" StringFormat="Rs.{0:N0}"/>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text="cash payments today" Foreground="{DynamicResource SubTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Online Payments -->
                <Border Style="{DynamicResource CardBorder}" Margin="6,0,0,0">
                    <StackPanel>
                        <TextBlock Text="🌐" FontSize="26" Margin="0,0,0,8"/>
                        <TextBlock Text="Online Payments" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource SubTextBrush}"/>
                        <TextBlock FontSize="22" FontWeight="Bold" Foreground="#0EA5E9" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <Binding Path="TodayOnlinePayments" StringFormat="Rs.{0:N0}"/>
                            </TextBlock.Text>
                        </TextBlock>
                        <TextBlock Text="online payments today" Foreground="{DynamicResource SubTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Two Column Layout -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Recent Sales -->
                <Border Grid.Column="0" Style="{DynamicResource CardBorder}" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="📋 Recent Sales" FontSize="16" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}" Margin="0,0,0,14"/>

                        <DataGrid ItemsSource="{Binding RecentSales}" Style="{DynamicResource ModernDataGrid}"
                                  ColumnHeaderStyle="{DynamicResource ModernDataGridColumnHeader}"
                                  RowStyle="{DynamicResource ModernDataGridRow}"
                                  CellStyle="{DynamicResource ModernDataGridCell}"
                                  MaxHeight="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Invoice" Binding="{Binding InvoiceNumber}" Width="70"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="10,0"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="*** RETURN BILL ***">
                                                    <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="Total" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding GrandTotal, StringFormat='Rs.{0:N0}'}" VerticalAlignment="Center" Padding="10,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <!-- Color-code returns based on Status -->
                                                            <DataTrigger Binding="{Binding Status}" Value="*** RETURN BILL ***">
                                                                <Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/>
                                                                <Setter Property="FontWeight" Value="Bold"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Time" Binding="{Binding SaleDate, StringFormat='dd/MM HH:mm'}" Width="Auto"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>


                <!-- Low Stock List -->
                <Border Grid.Column="1" Style="{DynamicResource CardBorder}" Margin="12,0,0,0">
                    <StackPanel>
                        <TextBlock Text="⚠️ Low Stock Alert" FontSize="16" FontWeight="SemiBold"
                                   Foreground="#F44336" Margin="0,0,0,14"/>
                        <DataGrid ItemsSource="{Binding LowStockItems}" Style="{DynamicResource ModernDataGrid}"
                                  ColumnHeaderStyle="{DynamicResource ModernDataGridColumnHeader}"
                                  RowStyle="{DynamicResource ModernDataGridRow}"
                                  CellStyle="{DynamicResource ModernDataGridCell}"
                                  MaxHeight="300">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Item" Binding="{Binding Description}" Width="*"/>
                                <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="Auto"/>
                                <DataGridTextColumn Header="Min" Binding="{Binding MinStockThreshold}" Width="Auto"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

        </StackPanel>
    </ScrollViewer>
</UserControl>
